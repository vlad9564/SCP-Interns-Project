ID: TestDeploy
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: frontend
   type: html5
   path: frontend
   parameters:
      disk-quota: 256M
      memory: 256M
   build-parameters:
      builder: npm
   requires:
    - name: uaa_Frontend
#     - name: dest_Frontend

resources:
 - name: uaa_Frontend
   parameters:
      path: ./xs-security.json
      service-plan: application
      service: xsuaa
   type: org.cloudfoundry.managed-service

#  - name: dest_Frontend
#    parameters:
#       service-plan: lite
#       service: destination
#    type: org.cloudfoundry.managed-service

